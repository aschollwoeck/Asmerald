flowchart LR

Root -->|Abs| Abs
Root -->|Changes| Changes
Root -->|Char| Char
Root -->|Coalesce| Coalesce
Root -->|Format| Format
Root -->|Glob| Glob
Root -->|Hex| Hex
Root -->|IfNull| IfNull
Root -->|IfNull| IfNullY
Root -->|Iif| Iif
Root -->|Instr| Instr
Root -->|LastInsertRowId| LastInsertRowId
Root -->|Length| Length
Root -->|Like| Like
Root -->|LikeEscape| LikeEscape
Root -->|Likelihood| Likelihood
Root -->|Likely| Likely
Root -->|Lower| Lower
Root -->|LTrim| LTrim
Root -->|LTrim| LTrimY
Root -->|Max| Max
Root -->|Min| Min
Root -->|NullIf| NullIf
Root -->|Quote| Quote
Root -->|Random| Random
Root -->|Randomblob| Randomblob
Root -->|Replace| Replace
Root -->|Round| Round
Root -->|Round| RoundDigits
Root -->|RTrim| RTrim
Root -->|RTrim| RTrimY
Root -->|Sign| Sign
Root -->|Soundex| Soundex
Root -->|Substr| SubstrLength
Root -->|Substr| Substr
Root -->|Substring| SubstringLength
Root -->|Substring| Substring
Root -->|TotalChanges| TotalChanges
Root -->|Trim| Trim
Root -->|Trim| TrimY
Root -->|Typeof| Typeof
Root -->|Unicode| Unicode
Root -->|Unlikely| Unlikely
Root -->|Upper| Upper
Root -->|Zerblob| Zerblob