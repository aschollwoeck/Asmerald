# TypesafeSql.Generate
Console application to generate classes of an database to use for TypesafeSql.

## Modes
- Database information
- Code generation

## Example
```bash
generate SQLite "Data Source=<path>.sqlite3;" "<path to folder>" --nspace=TestNamespace
```